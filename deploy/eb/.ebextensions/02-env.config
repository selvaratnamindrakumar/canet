option_settings:
  aws:elasticbeanstalk:application:environment:
    SPRING_PROFILES_ACTIVE: aws
    # These are overridden by environment-specific values set during deployment:
    # AWS_REGION, DYNAMO_TABLE, AURORA_ENDPOINT, AURORA_USER, AURORA_PASSWORD
    # Optional: ORACLE_ENDPOINT, ORACLE_USER, ORACLE_PASSWORD, ORACLE_SERVICE
